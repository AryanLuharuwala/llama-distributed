build/
build-*/
/install/
.cache/
compile_commands.json
__pycache__/
*.pyc
*.o
*.a
*.so
*.dylib
*.exe
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile

# Server build outputs (checked-in binaries are a footgun)
server/server
server/distpool-server
server/dist-server

# Local database + WAL — may contain tokens, user data
server/*.sqlite
server/*.sqlite-shm
server/*.sqlite-wal
/distpool.sqlite
/distpool.sqlite-shm
/distpool.sqlite-wal

# Local-only built binaries (built via scripts/build.sh, not source)
/cmd/dist-turn/dist-turn
/cmd/surd/surd

# Model weights — large, not source
server/models-store/

# Env / secrets
.env
.env.*
!.env.example
azure_creds
azure_creds.json

# Editor / OS
.DS_Store
.idea/
.vscode/
.claude/

# Bazel (P19) — symlinked build outputs + bzlmod resolved lockfile
bazel-*
MODULE.bazel.lock
